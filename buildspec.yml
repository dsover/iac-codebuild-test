version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - yarn
  pre_build:
    commands:
      - cd lambda
      - yarn
      - yarn test
  build:
    commands:
      - yarn demo:apply